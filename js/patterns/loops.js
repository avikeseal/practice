//while and do loops:

let number = 0;
//everytime the loop repeats 
//number gets a value more that is two more than it's 
//previous value
while (number <= 12) {
    console.log(number);
    //at the start of every repetition
    //it is compared to 12 to decide whether
    //the program's work is done
    number += 2;
}